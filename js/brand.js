// Brand identity. Drop the foundation's real assets in here; the header, sign-in screen and
// `tools/make-icons.mjs` all read from this file. Leave a field null to use the khatam fallback.
export const brand = {
  name: 'Avicenna',
  shortName: 'Avicenna',
  organisation: 'Avicenna Foundation',
  // Inline SVG string for the logo mark (square, uses currentColor). null = khatam star.
  markSvg: null,
  // Inline SVG string for the horizontal wordmark (uses currentColor). null = letter-spaced text.
  wordmarkSvg: null,
};
